<Window x:Class="Scada_Demo.Other_Settings.Auto_BackupSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Scada_Demo.Other_Settings"
        mc:Ignorable="d"
        Title="Auto Backup Interval Setup"
        Height="410" Width="350"
        Background="#0D3A3A"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <TextBlock Text="Select Database Backup Interval"
                   Margin="5"
                   Style="{StaticResource HeaderTextStyle}"/>

        <!-- CONTENT -->
        <Border Grid.Row="1"
        Margin="5"
        Padding="20"
        Background="{StaticResource PanelBrush}"
        BorderBrush="{StaticResource BorderBrushColor}"
        BorderThickness="2"
        CornerRadius="6">

            <Grid>
                <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center">

                    <RadioButton Content="Daily"
                         GroupName="Backup"
                         Margin="10"
                         FontSize="16"
                         Foreground="White"
                         IsChecked="True"/>

                    <RadioButton Content="Weekly"
                         GroupName="Backup"
                         Margin="10"
                         FontSize="16"
                         Foreground="White"/>

                    <RadioButton Content="Fortnightly"
                         GroupName="Backup"
                         Margin="10"
                         FontSize="16"
                         Foreground="White"/>

                    <RadioButton Content="Monthly"
                         GroupName="Backup"
                         Margin="10"
                         FontSize="16"
                         Foreground="White"/>

                </StackPanel>
            </Grid>

        </Border>

        <!-- BUTTONS -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,15">

            <Button Content="Save"
                    Width="120"
                    Margin="10"
                 />

            <Button Content="Exit"
                    Width="120"
                    Margin="10"
                    Style="{StaticResource RedButton}"/>

        </StackPanel>

    </Grid>
</Window>