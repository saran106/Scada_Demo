<Window x:Class="Scada_Demo.User.Delete_User"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Scada_Demo.User"
        mc:Ignorable="d"
        Title="Delete_User" Height="300" Width="500"
Background="#0D3A3A"
WindowStartupLocation="CenterScreen">

    <Grid Margin="10">

        <!-- MAIN ROWS -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <TextBlock Text="Delete user"
                   Style="{StaticResource HeaderTextStyle}"
                   Margin="10,5"/>

        <!-- CONTENT -->
        <Border Grid.Row="1"
                Margin="10"
                Padding="20"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrushColor}"
                BorderThickness="2"
                CornerRadius="6">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <!-- LABEL -->
                <TextBlock Text="User name"
                           VerticalAlignment="Center"
                           Style="{StaticResource CompactLabel}"/>

                <!-- COMBO -->
                <ComboBox Grid.Column="1"
                          Width="220"
                          Height="34"
                          Style="{StaticResource CompactComboBoxSmall}"/>

            </Grid>
        </Border>

        <!-- BUTTONS -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,10">

            <Button Content="Delete"
                    Width="120"
                    Margin="10"/>

            <Button Content="Exit"
                    Width="120"
                    Margin="10"
                    Style="{StaticResource RedButton}"/>

        </StackPanel>

    </Grid>
</Window>
